module github.com/xbcio/xbc/extensions/authorization/rbac

go 1.25.0

// github.com/xbcio/xbc is deliberately absent until core has a real release
// tag. Repository development resolves it through go.work; adding a local
// replace or placeholder version would make this independently published
// extension unusable downstream.
